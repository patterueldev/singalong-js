import React from "react";
import { View } from "react-native";

export default function App() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      {/* TODO: Implement player app with 4-layer architecture */}
      {/* Layer 1: Full-screen video player */}
      {/* Layer 2: Scoring screen overlay */}
      {/* Layer 3: UI overlays (queue, PSA, QR code) */}
      {/* Layer 4: Player controls (play/pause/seek/volume) */}
    </View>
  );
}
