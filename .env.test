# Test Environment Configuration
NODE_ENV=test
PORT=3000

# MongoDB (test database on different port)
MONGODB_URI=mongodb://admin:admin@localhost:27018/singalong-test?authSource=admin
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=admin

# MinIO (test bucket on different ports)
MINIO_ENDPOINT=http://localhost:9002
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=singalong-test

# Default Admin (for testing)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=P@ssw0rd!

# External APIs (use test/mock keys)
MEDIA_PROVIDER_API_KEY=test-key
OPENAI_API_KEY=test-key

# Data paths
MONGODB_DATA_PATH=./data/mongodb-test
